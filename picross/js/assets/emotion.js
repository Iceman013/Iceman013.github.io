export class Emotion {
    /**
     * 
     * @param {number} id 
     * @param {String} name 
     * @param {String} url 
     */
    constructor(id, name, url) {
        this.id = id;
        this.name = name;
        this.url = url;
    }
}