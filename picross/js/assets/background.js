export class Background {
    /**
     * 
     * @param {number} id 
     * @param {String} name 
     * @param {String} shortName
     * @param {String} url 
     */
    constructor(id, name, shortName, url) {
        this.id = id;
        this.name = name;
        this.shortName = shortName;
        this.url = url;
    }
}