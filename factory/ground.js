function Ground(id, name, frequency, cluster, speed, img) {
    this.id = id;
    this.name = name;
    this.frequency = frequency;
    this.cluster = cluster;
    this.speed = speed;
    this.img = img;
}