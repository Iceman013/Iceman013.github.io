import { Role } from "../role.js";
import { Category } from "../category.js";
import { Message, getMessages } from "../../../js/message.js";

export async function pickRole(hostDetails, category) {
    let output = [
        {
            name: "Bab",
            role: "Cool",
            points: 2,
        },
    ];
    return output;
}